Step back and ask the smart strategic questions before planning what's next. Data-backed, assumption-interrogating, presents options as combinations not choices.

Load the skill file at `~/.claude/skills/qq-smart-next-move/SKILL.md` and follow its instructions.

Context: $ARGUMENTS

If args provided: use them as context for what just happened.
If no args: infer from conversation context and recent session state.
